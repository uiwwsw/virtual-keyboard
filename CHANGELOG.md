# Changelog

## 2.2.0

- 입력란에 처음 포커스하거나 커서를 옮기면 키패드 상단에서 바로 붙여넣을 수 있습니다. 선택한 텍스트의 도구도 복사·잘라내기·붙여넣기로 연결하고 전체 선택은 편집 화면에 유지했습니다.
- 붙여넣을 위치를 네이티브 선택 범위에서 읽고 삽입 후 커서·포커스를 입력란에 유지합니다. 선택 교체와 한 번의 실행 취소를 지원합니다.
- 클립보드 읽기 진행 상태와 중복 실행 방지를 추가했습니다. 대기 중 커서·입력값·정책·활성 입력란이 바뀌거나 기본 붙여넣기가 실행되면 오래된 결과를 반영하지 않습니다.
- 빈 클립보드·필터 거절·길이 제한·부모의 값 거절을 구분해 안내합니다. 읽기 권한 실패 시 현재 선택 범위를 유지하고 기본 붙여넣기를 안내합니다.

## 2.1.0

- 입력란을 실제 `<input inputMode="none">`으로 전환했습니다. 커서 이동, 길게 누르기, 선택 손잡이와 기본 편집 메뉴를 브라우저가 처리합니다.
- 자체 커서, 포인터 좌표 계산과 550ms 길게 누르기 타이머를 제거했습니다. 누르는 동안 브라우저가 만든 선택 범위를 손을 뗄 때 덮어쓰지 않습니다.
- 네이티브 선택 방향을 한글 조합·선택 교체·복사·잘라내기·실행 취소와 연결했습니다. 브라우저 입력과 IME 조합에도 필터·길이 제한·외부 값 초기화를 적용합니다.
- 입력란 여백 전체를 실제 터치 영역으로 사용하고, 닫기 후 재탭하면 키패드가 다시 열리도록 포커스를 해제합니다. 선택 상태가 같으면 키패드를 불필요하게 다시 렌더링하지 않습니다.
- 컨테이너 스타일과 공개 값·ref API는 유지하며 `id`·접근성 속성은 실제 input에 적용합니다. 내부 문자 span을 참조하던 코드는 input 선택 API로 변경해야 합니다.

## 2.0.1

- 기본 삭제 키를 글꼴에 의존하지 않는 SVG 아이콘으로 교체했습니다. 사용자 지정 라벨은 유지합니다.
- 입력의 선택 영역을 브라우저 Selection API와 양방향으로 연결했습니다. 마우스 드래그, 단어 선택, 선택 범위 조절과 기본 복사 메뉴를 차단하지 않습니다.
- 글자를 선택하면 현재 자판 위에 복사·잘라내기·전체 선택을 바로 표시합니다. 복사 완료 피드백을 추가하고, 선택이 없을 때 복사·잘라내기는 비활성화합니다.
- Clipboard API를 사용할 수 없는 환경에서 실제 선택 영역의 기본 복사 이벤트를 대체 경로로 사용합니다.
- Safari에서 연속 삭제 직후 발생할 수 있는 오래된 DOM 선택 위치를 무시하며, 선택 방향·한글·이모지 경계를 보존합니다.

## 2.0.0

### Breaking changes

- React 18 이상을 지원합니다. React 17 지원 표기를 제거했습니다.
- 입력란의 내부 Canvas/custom element 구조를 DOM 텍스트로 변경했습니다. 입력란 스타일은 `className` 또는 `style`로 적용하세요.
- 데모 빌드 출력은 `demo-dist/`, npm 패키지는 `dist/`입니다. Vercel 설정을 함께 업데이트했습니다.

### Input and keyboard

- 탭·스크롤·길게 누르기를 구분하고, 취소된 터치와 다중 터치가 커서·편집 모드를 잘못 바꾸지 않도록 개선했습니다.
- 화면 키와 툴바는 손을 뗄 때 실행하며, 키 밖으로 이동하면 취소하고 반복 삭제도 멈춥니다.
- 단어 길게 누르기 선택, 한글 편집 도구 이름, 가로 화면의 44px 키 높이, 데모의 터치 영역과 안내 문구를 개선했습니다.
- 키보드를 화면 아래에 바로 배치하고 입력란의 자동 스크롤이 길게 누르기와 충돌하지 않도록 수정했습니다.
- 키 사이 여백과 툴바를 터치해도 입력 포커스를 유지하여 모바일 연속 입력 중 키보드가 닫히는 문제를 수정했습니다.
- 언어 전환 키를 테마에 맞는 단색 아이콘과 현재 언어(한 / EN) 표시로 정리했습니다.
- 한글 조합·자모 삭제, 커서·선택 영역, 이모지·결합 문자, 제어 값 초기화를 개선했습니다.
- 특수키 오입력과 사용자 키의 label/value 혼동을 수정했습니다.
- 숫자·기호 자판, 전체 선택, 실행 취소·다시 실행을 추가했습니다.
- `onValueChange`, `disabled`, `readOnly`, `maxLength`, `onClipboardError`와 공개 ref API를 제공합니다.
- 브라우저 클립보드 실패, 반복 입력 타이머, 다중 Provider 포커스와 언어 설정을 정리했습니다.
- 화면 키패드의 데스크톱 표시, 시스템·명시적 테마, safe area, 터치·키보드 조작을 지원합니다.

### Website and distribution

- 7개 모드를 체험하는 반응형 데모, 라이트·다크 테마, 값·코드 예제, 전체 예제 복사를 제공합니다.
- 상시 Canvas 렌더링 루프를 제거하고 실제 버튼과 DOM 텍스트로 접근성을 높였습니다.
- npm JavaScript 번들·타입 선언, NodeNext 소비자, SSR 및 실제 tarball 검증을 추가했습니다.
- CI가 포맷·린트·테스트·배포 빌드·접근성을 검사합니다. registry 확인에 실패하면 npm 게시를 중단합니다.
